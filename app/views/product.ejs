<!DOCTYPE html>
<html lang="fr">

<head>
  <!-- Meta -->
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="<%= coffee.nom %> - Détails du café oCoffee">

  <!-- Title -->
  <title><%= coffee.nom %> - oCoffee</title>

  <!-- Common styles -->
  <link rel="stylesheet" href="../css/reset.css">
  <link rel="stylesheet" href="../css/base.css">
  <link rel="stylesheet" href="../css/common.css">
  <link rel="stylesheet" href="../css/layout.css">

  <!-- Common scripts -->
  <script defer src="../js/header.js"></script>

  <!-- Style -->
  <link rel="stylesheet" href="../css/product.css">
</head>

<body>
  <%- include('partials/header') %>

  <main>
    <section id="product-detail">
      <div class="product-container">
        <div class="product-image">
          <img src="../images/coffees/<%= coffee.reference %>.png" alt="<%= coffee.nom %>">
        </div>
        
        <div class="product-info">
          <h1><%= coffee.nom %></h1>
          
          <div class="product-details">
            <div class="price">
              <span class="price-value"><%= coffee.prix_kg %>€</span>
              <span class="price-unit">/kg</span>
            </div>
            
            <% if (coffee.origine) { %>
              <div class="origin">
                <h3>Origine</h3>
                <p><%= coffee.origine %></p>
              </div>
            <% } %>
            
            <% if (coffee.description_cafe) { %>
              <div class="characteristics">
                <h3>Caractéristiques</h3>
                <p><%= coffee.description_cafe %></p>
              </div>
            <% } %>
            
            <div class="availability">
              <% if (coffee.disponibilite) { %>
                <span class="available">✅ Disponible</span>
              <% } else { %>
                <span class="unavailable">❌ Indisponible</span>
              <% } %>
            </div>
            
            <div class="reference">
              <small>Référence: <%= coffee.reference %></small>
            </div>
          </div>
          
          <div class="actions">
            <a class="button" href="/catalogue">← Retour au catalogue</a>
            <% if (coffee.disponibilite) { %>
              <button class="button" disabled>Commander (bientôt disponible)</button>
            <% } %>
          </div>
        </div>
      </div>
    </section>
  </main>

  <%- include('partials/footer') %>

</body>
</html>
