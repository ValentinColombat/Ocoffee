<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Catalogue - oCoffee</title>

    <!-- Styles CSS -->
    <link rel="stylesheet" href="/css/reset.css">
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">

    <!-- Modern styles -->
    <link rel="stylesheet" href="/css/base.css">
    <link rel="stylesheet" href="/css/layout.css">
    <link rel="stylesheet" href="/css/common.css">
    <link rel="stylesheet" href="/css/catalog.css">

    <!-- Scripts -->
    <script defer src="/js/header.js"></script>
    <script defer src="/js/catalog.js"></script>
  </head>

  <body>
    <%- include('partials/header') %>

    <main>
      <section id="catalog">
        <h1>Notre catalogue</h1>

        <div class="articles" id="articles-list">
          <% coffees.forEach(coffee => { %>
            <article>
              <% if (coffee.disponibilite) { %>
                <div class="availability-badge available">✓ Disponible</div>
              <% } else { %>
                <div class="availability-badge unavailable">✗ Indisponible</div>
              <% } %>
              
              <img
                src="/images/coffees/<%= coffee.reference %>.png"
                alt="<%= coffee.nom %>"
              />
              <div>
                <h2><%= coffee.nom %></h2>
                <p><%= coffee.origine %></p>
                <p><%= coffee.prix_kg %> €/kg</p>
                <% if (coffee.caracteristiques) { %>
                  <p><%= coffee.caracteristiques %></p>
                <% } %>
                <% if (!coffee.disponibilite) { %>
                  <p style="color: red;">⚠️ Non disponible actuellement</p>
                <% } %>
                <a class="btn btn-secondary btn-sm" href="/cafe/<%= coffee.reference %>">
                  👁️ Voir le détail
                </a>
              </div>
            </article>
          <% }) %>
        </div>

        <button class="btn btn-accent btn-lg" id="see-all-products-button">
          ✨ Voir tous les produits ✨
        </button>
      </section>
    </main>

    <%- include('partials/footer') %>

  </body>
</html>
